# Swagger Documentation Structure

هذا المجلد يحتوي على جميع إعدادات وتوثيق Swagger API منظمة بشكل منفصل ومنطقي.

## هيكل المجلد

```
src/swagger/
├── swagger.config.js          # التكوين الرئيسي لـ Swagger
├── responses.js               # تعريفات الاستجابات المشتركة
├── schemas/                   # مخططات البيانات
│   ├── index.js              # تصدير جميع المخططات
│   ├── user.schemas.js       # مخططات المستخدم
│   └── common.schemas.js     # المخططات المشتركة
├── docs/                     # توثيق نقاط النهاية
│   ├── auth.docs.js         # توثيق نقاط المصادقة
│   └── general.docs.js      # توثيق النقاط العامة
└── README.md                # هذا الملف
```

## الملفات والوظائف

### 📁 **schemas/** - مخططات البيانات

- **`user.schemas.js`**: يحتوي على مخططات المستخدم (User, UserSignup, UserLogin)
- **`common.schemas.js`**: المخططات المشتركة (SuccessResponse, ErrorResponse, LoginCredentials)
- **`index.js`**: يجمع ويصدر جميع المخططات

### 📁 **docs/** - توثيق نقاط النهاية

- **`auth.docs.js`**: توثيق شامل لنقاط المصادقة (/auth/signup, /auth/login)
- **`general.docs.js`**: توثيق النقاط العامة (/, /*)

### 📄 **الملفات الرئيسية**

- **`swagger.config.js`**: التكوين الرئيسي الذي يجمع كل شيء معاً
- **`responses.js`**: تعريفات الاستجابات المشتركة للأخطاء والحالات المختلفة

## كيفية إضافة توثيق جديد

### إضافة مخطط جديد

1. أنشئ ملف جديد في `schemas/` أو أضف للملفات الموجودة
2. أضف المخطط الجديد للتصدير في `schemas/index.js`

```javascript
// في schemas/new.schemas.js
export const newSchemas = {
  NewModel: {
    type: 'object',
    properties: {
      // خصائص المخطط
    }
  }
};

// في schemas/index.js
import { newSchemas } from './new.schemas.js';
export const allSchemas = {
  ...userSchemas,
  ...commonSchemas,
  ...newSchemas  // أضف هنا
};
```

### إضافة توثيق نقطة نهاية جديدة

1. أنشئ ملف جديد في `docs/` أو أضف للملفات الموجودة
2. أضف التوثيق للتكوين الرئيسي

```javascript
// في docs/new.docs.js
export const newDocs = {
  '/new-endpoint': {
    get: {
      summary: 'وصف النقطة',
      tags: ['NewTag'],
      // باقي التوثيق
    }
  }
};

// في swagger.config.js
import { newDocs } from './docs/new.docs.js';
// أضف للـ paths
paths: {
  ...authDocs,
  ...generalDocs,
  ...newDocs  // أضف هنا
}
```

### إضافة استجابة مشتركة جديدة

```javascript
// في responses.js
export const commonResponses = {
  // الاستجابات الموجودة...
  NewResponse: {
    description: 'وصف الاستجابة الجديدة',
    content: {
      'application/json': {
        schema: {
          // مخطط الاستجابة
        }
      }
    }
  }
};
```

## المزايا

✅ **تنظيم أفضل**: كل شيء في مكانه المناسب  
✅ **سهولة الصيانة**: تعديل أي جزء دون التأثير على الباقي  
✅ **قابلية إعادة الاستخدام**: المخططات والاستجابات قابلة للاستخدام المتكرر  
✅ **وضوح أكبر**: الكود أصبح أكثر وضوحاً وأقل تعقيداً  
✅ **سهولة التطوير**: إضافة توثيق جديد أصبح أسهل  

## الاستخدام

يتم استيراد التكوين الرئيسي في `app.controller.js`:

```javascript
import swaggerSpecs from './swagger/swagger.config.js'
```

ويمكن الوصول للتوثيق عبر:
```
http://localhost:{PORT}/api-docs
```
